{
  "weibo_urls": {
    "type": "list",
    "item_type": "string",
    "default": [],
    "description": "微博用户主页链接",
    "hint": "可以填写多个，例如: https://weibo.com/u/1234567890"
  },
  "check_interval": {
    "type": "int",
    "default": 10,
    "description": "检查间隔（分钟）"
  },
  "check_interval_jitter": {
    "type": "int",
    "default": 2,
    "description": "检查间隔随机浮动（分钟）",
    "hint": "实际检查间隔会在 [间隔-浮动, 间隔+浮动] 范围内随机选择。例如：间隔10，浮动2，则随机在 8-12 分钟内检查一次。"
  },
  "request_interval": {
    "type": "int",
    "default": 5,
    "description": "账号请求间隔（秒）",
    "hint": "多个账号间抓取的间隔时间，避免请求过快被拦截"
  },
  "request_interval_jitter": {
    "type": "int",
    "default": 1,
    "description": "请求间隔随机浮动（秒）",
    "hint": "实际请求间隔会在 [间隔-浮动, 间隔+浮动] 范围内随机选择。设置为0则不使用随机性。"
  },
  "target_conversation_id": {
    "type": "list",
    "item_type": "string",
    "default": [],
    "description": "推送目标会话 ID",
    "hint": "可以填写多个。通过指令 /get_umo 获取当前会话 ID"
  },
  "weibo_cookie": {
    "type": "string",
    "default": "",
    "description": "微博 Cookie",
    "hint": "必须配置微博 Cookie 才能正常抓取数据，确保稳定性"
  },
  "message_format": {
    "type": "string",
    "default": "🔔 {name} 发微博啦！\n\n{weibo}\n\n链接: {link}",
    "description": "推送消息格式",
    "hint": "支持变量: {name} (用户名), {weibo} (内容), {link} (链接)"
  },
  "filter_keywords": {
    "type": "list",
    "item_type": "string",
    "default": [],
    "description": "屏蔽词列表",
    "hint": "如果微博正文包含这些词，将不会被推送。例如: 推广, 广告"
  },
  "send_original": {
    "type": "bool",
    "default": true,
    "description": "是否推送原创微博"
  },
  "send_forward": {
    "type": "bool",
    "default": true,
    "description": "是否推送转发微博"
  },
  "weibo_screenshot": {
    "type": "bool",
    "default": true,
    "description": "是否发送微博网页截图",
    "hint": "开启后将截取微博页面并随消息一起发送"
  },
  "screenshot_full_page": {
    "type": "bool",
    "default": false,
    "description": "是否截取完整长图",
    "hint": "开启后会截取整个网页的长图，关闭则仅截取当前屏幕大小"
  },
  "screenshot_width": {
    "type": "int",
    "default": 1280,
    "description": "截图宽度",
    "hint": "默认 1280"
  },
  "screenshot_height": {
    "type": "int",
    "default": 720,
    "description": "截图高度",
    "hint": "默认 720"
  },
  "screenshot_format": {
    "type": "string",
    "default": "jpeg",
    "description": "截图格式",
    "hint": "可选填写 jpeg 或 png"
  },
  "screenshot_quality": {
    "type": "int",
    "default": 80,
    "description": "截图压缩质量",
    "hint": "仅对 jpeg 格式有效，推荐保持 80 即可"
  },
  "screenshot_wait_time": {
    "type": "int",
    "default": 2000,
    "description": "截图等待时间 (毫秒)",
    "hint": "截图前等待网页加载的时间，2000 即代表等待 2 秒，防止截到白屏"
  }
}